model_list:
  # ── Chat model ─────────────────────────────────
  - model_name: chat
    litellm_params:
      model: ollama_chat/llama3.1
      api_base: http://ollama:11434

  # ── Embedding model (used by ChromaDB + memory) ─
  - model_name: text-embedding
    litellm_params:
      model: ollama/nomic-embed-text
      api_base: http://ollama:11434

general_settings:
  master_key: sk-local-dev
